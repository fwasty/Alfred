# 2026-03-20

## GuruScan Project — Session Summary

### Deployment
- **Vercel doesn't work** — `better-sqlite3` native module is incompatible with serverless runtime. Tried multiple approaches (serverExternalPackages, outputFileTracingIncludes, /tmp copy, sql.js WASM) — none worked.
- **Deployed on this VPS instead:**
  - Next.js production build on port 3200
  - systemd service: `guruscan.service` (enabled, auto-restart)
  - nginx reverse proxy on port 80
  - Cloudflare DNS (proxied, orange cloud) + SSL mode **Flexible**
  - Domain: **guruscan.xyz** → A records → `187.77.26.215`
- Site is live and stable (ran 6+ hours without issues)

### GitHub Setup
- Repo: `fwasty/Alfred` — PAT configured for push
- Workflow: edit code → `next build` → `systemctl restart guruscan` → `git push`

### Data Cleanup Done
- Hidden **135 duplicate courses** (same guru + same name)
- Hidden **15 duplicate guru pairs** (same person, different handles)
- Merged overlapping categories: E-Commerce→Ecom, Sports/Sports Picks→Sports Betting, AI/Agency→AI, Reselling→Ecom, UGC→Creator
- Auto-categorized **261 of 365 "Whop" gurus** using course name keywords; remaining 99 → "Other"
- Final: **745 visible gurus**, **1394 visible courses**, **13 clean categories**
- DB backup: `gurusan.db.bak_2026-03-19_cleanup`

### UI Changes Made
1. **Light/Dark mode toggle** — 🌙/☀️ button in nav, persists in localStorage
2. **Dark mode fixed** — true black background (#09090b), dark cards, dark text
3. **Sticky transparent nav** — backdrop-blur-xl, semi-transparent, stays on scroll
4. **Rating distribution bars** — 5★→1★ breakdown chart on guru profile pages
5. **Theme-aware components** — Cards, buttons, badges, homepage all use CSS variables
6. **Course card rating badge** — dark glass pill instead of white box
7. **CategoryFilter component** — built but not wired to homepage yet (awaiting approval)

### UX Research Done
- Full report saved at `/root/.openclaw/workspace/guruscan-ux-research.md`
- Analyzed RateMyProfessor, Trustpilot, G2
- Key recommendations: search-first homepage, structured reviews, rating distribution bars, tags system, AI review summaries, "would recommend %" metric

### Seth's Design Preferences
- Wants to **improve** existing design, not replace it
- Wants it "professional and clean"
- Asked for mockups of different designs — directed to try `?theme=paper/midnight/glass/mono/editorial`
- Wants dark mode darker (fixed)
- Wants sticky transparent nav (fixed)
- Waiting for his feedback on which theme direction + what feels "too plain"

### Existing Themes Available
- `paper` — light with subtle purple/pink gradients (default)
- `midnight` — true dark mode, green accent
- `glass` — colorful pastel gradients
- `mono` — clean black & white
- `editorial` — professional, slate tones

### Technical Notes
- Seth's Cloudflare: guruscan.xyz, SSL Flexible, proxy ON
- GitHub PAT: stored in git remote URL (fwasty/Alfred)
- Anthropic model: running on Haiku (cheap), Opus as fallback
- Seth's timezone: US Central (America/Chicago)
- It's ~11pm CT for Seth right now

### Cost Estimate
- This session used significant tokens due to code reading/editing
- Estimated: ~$0.15-0.25 on Haiku for the full session
